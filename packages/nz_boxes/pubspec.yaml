name: nz_boxes
description: "A new Flutter package project."
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  nz_common:
    path: ../nz_common
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_lints: ^4.0.0

